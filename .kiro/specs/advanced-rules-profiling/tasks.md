# 実装計画: 高度ルール個別プロファイル

## タスク

- [x] 1. 計測用データ構造を追加
  - ルール名・実行時間・診断件数・成否・エラー文言を保持する型を用意
  - ルール別計測が無効な場合は生成しない

- [x] 2. AdvancedRulesManager に計測フックを追加
  - `checkText` と `checkWithRules` でルール実行前後を計測
  - 例外発生時も計測結果に失敗情報を残す
  - 既存の診断結果の生成フローは維持する

- [x] 3. 解析ログ出力を追加
  - `server/src/main.ts` の高度ルール評価後にログブロックを出力
  - 実行時間の降順で整列し、比率と件数を表示
  - 文書URI・バージョンをヘッダに含める

- [x] 4. ログ有効化条件の統一
  - `isProfileLogsEnabled()` が false の場合は計測処理をスキップ
  - 既存のプロファイル出力と同じ条件で動くことを確認

- [x] 5. 手動確認
  - `OTAK_LCP_PROFILE=1` でREADMEを解析し、ルール別ログが出力されることを確認
  - Jestテストで全10件のプロファイリングテストが成功（自動化された検証）
